{
  "private": false,
  "name": "reviews.nirjan.net",
  "version": "3.1.0",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev:astro": "cd ./astro && npm run dev",
    "dev:sanity": "cd ./sanity && npm run start",
    "dev": "run-p dev:*",
    "commit": "cz"
  },
  "devDependencies": {
    "commitizen": "^4.2.6",
    "cz-conventional-changelog": "^3.3.0",
    "npm-run-all": "^4.1.5"
  },
  "config": {
    "commitizen": {
      "path": "./node_modules/cz-conventional-changelog"
    }
  }
}
